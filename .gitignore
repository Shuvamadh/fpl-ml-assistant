# Large/regenerable data (rebuilt by src/collect_history.py, src/features.py,
# src/predict.py -- not needed to run the app, only to retrain the model)
data/raw/
data/raw.zip
data/history_gws.csv
data/features.parquet
data/players_raw_2526.csv
data/*.json
data/player_predictions.csv
data/gui_config.json

# Small, cheap-to-regenerate but not essential to ship either
models/two_stage_meta.json.bak

# Python
__pycache__/
*.pyc
.venv/
venv/

# Local run artifacts
*.log
.streamlit/secrets.toml
